apiVersion: batch/v1
kind: Job
metadata:
  name: job-wq-1
spec:
  completions: 8
  parallelism: 2
  template:
    metadata:
      name: job-wq-1
    spec:
      containers:
      - name: c
        image: dockerhub.ebi.ac.uk/soumyadip/dsds-indexer
        envFrom:
         - configMapRef:
             name: indexer-config
      imagePullSecrets:
      - name: gitlab-registry-secret
      restartPolicy: OnFailure